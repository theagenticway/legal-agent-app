// frontend/js/global.js

document.addEventListener('DOMContentLoaded', () => {
    feather.replace(); // Initialize Feather icons on all pages
});

// You can add other global utilities here if needed, like a general fetch wrapper.